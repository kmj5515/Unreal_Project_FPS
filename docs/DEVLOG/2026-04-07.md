# DEVLOG - 2026-04-07

## 오늘 목표
- Phase 0 완료 확인
- Phase 1(코어/캐릭터 골격) 시작 및 기본 동작 확인

## 작업 내용
- C++ 클래스 추가:
  - `AFPSGameMode`
  - `AFPSPlayerController`
  - `AFPSPlayerState`
  - `ABaseFPSCharacter`
  - `AAgentACharacter`
  - `AAgentBCharacter`
- `DefaultEngine.ini` 설정:
  - `GlobalDefaultGameMode=/Script/Ark.FPSGameMode`
- `ABaseFPSCharacter`:
  - 1인칭 카메라 컴포넌트 추가
  - Enhanced Input 바인딩(`Move`, `Look`, `Jump`) 추가
  - 이동 로직을 컨트롤러 Yaw 기준으로 적용

## 이슈 / 해결
- 증상: `A/D`는 동작하지만 `W/S` 미동작
- 원인: 입력 데이터 에셋에서 `IA_Move` 타입을 `Axis2D(Vector2D)`로 설정하지 않음
- 해결: `IA_Move`를 `Axis2D(Vector2D)`로 수정하여 정상 동작

## 입력 기준값 기록 (재발 방지)
- `IA_Move`: `Axis2D`
- `IMC_Default`:
  - `W -> Y +1`
  - `S -> Y -1`
  - `A -> X -1`
  - `D -> X +1`

## 현재 상태
- Phase 0: 완료
- Phase 1: 진행 중 (BP 팔 메쉬 연결 남음)

## 다음 액션
1. BP 팔 메쉬/애님 연결
2. Phase 2: `AFPSPlayerState`에 ASC + AttributeSet 추가
3. Character-PlayerState GAS 초기화 파이프라인 연결
