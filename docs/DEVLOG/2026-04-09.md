# DEVLOG - 2026-04-09

## 오늘 작업 요약
- `GE_DefaultAttributes` 초기화 경로를 단일화함.
  - `DT_CharacterDefaults`/SetByCaller 기본 스탯 경로 제거
  - 서버에서 `TryApplyDefaultAttributes()`로 `DefaultAttributesEffect` 1회 적용 유지
- 무기 트레이스 디버그 가시화 추가.
  - `AWeaponBase`: `bDebugDrawTrace`, `DebugDrawDuration`
  - 히트 시 초록, 미스 시 빨강 라인/마커 표시
- 히트 판정 보강.
  - 기존 `ECC_Visibility` 단일 판정에서
  - `ECC_Visibility + ECC_Pawn` 동시 판정 후 가까운 히트 채택으로 변경
- `Data.Damage` 관련 런타임 오류 원인 확인.
  - 원인: `GE_Damage` SetByCaller 태그/세팅 불일치 시 magnitude 미설정 에러 발생
  - 조치: 코드 강제 보정 없이 누락 시 로그로 원인 드러내는 방향 유지
- 초기 체력 `1` 문제 수정.
  - 원인: 기본값(`MaxHealth=1`) 상태에서 `Health`가 먼저 적용되면 clamp로 `1` 고정
  - 수정: `MaxHealth` 갱신 시 초기 구간(`Health <= 1`)은 `Health = MaxHealth`로 보정

## 검증 메모
- 로그 기준 `Miss` 위주에서 `Hit` 로그로 전환 확인
- 피격 시 `Health` 감소는 `ShowDebug AbilitySystem`로 추가 확인 필요

## 다음 액션
- `GA_WeaponReload` + `IA_Reload` 추가
- `State.Reloading` 태그를 실제 리로드 상태와 연결
